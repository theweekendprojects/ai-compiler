{
  "name": "@ai-compiler/e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "screenshots": "playwright test --grep @screenshot"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
